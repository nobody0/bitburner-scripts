# Repository layout

Four parts, one rule each:

| Directory | Rule |
|---|---|
| `game/`   | The ONLY code that reaches the game. Entrypoints listed in `bitburner.config.json` (validation rejects sources outside `game/`), bundled standalone by esbuild, pushed over the Remote File API. `start.ts` is the startup script only; the loop is `lib/controller.ts`, the game-state store is `lib/state.ts`, acquisition is `lib/probe-runner.ts` + `lib/probes/`, the write half is `lib/features/`, and `lib/telemetry*.ts` is the optional send. Plus `lib/dodge*.ts` and the puppet worker. |
| `shared/` | Pure code used by two or more parts: telemetry schema, the WorldView/Action/Planner seam, goals + the stream reducer, strategy. No ns, no Bun APIs, no `__TELEMETRY__`. |
| `sim/`    | The test loop. `vendor/bitburner/` (generated by `bun run vendor` from the pinned tag — DO NOT EDIT), `core/` (transcribed effects, mocks, rng), `clock.ts`, `world.ts`, `run.ts`, `compare.ts`, and simulator-specific tests under `tests/`. Runs under Bun only. |
| `ui/`     | The external UI process: telemetry hub (`server.ts`, port 12526) + browser viewer. The viewer is TypeScript under `app/` (shell + `project.ts` + one module per feature tab), bundled on demand by `Bun.build` inside the hub and served as `/app.js`; `public/index.html` is the static shell and stylesheet. No separate build step — the hub rebuilds when a source file changes, so a refresh is the whole dev loop. Runs under Bun only. |

Plus: `tools/` (build/sync/vendor tooling), `spec/` (these documents),
`tests/` (cross-project and tooling tests), `types/` (game-provided
NetscriptDefinitions), `runs/` (gitignored
JSONL telemetry, one file per run).

## Daily commands

```
bun run dev          # sync-on-save to the game (telemetry ON)
bun run dev:perf     # same, telemetry compiled out
bun run ui           # telemetry hub + viewer at http://127.0.0.1:12526
bun run sim -- --goal earn:1e6 --seed 1     # simulate; JSONL lands in runs/
bun run sim:compare runs/a.jsonl runs/b.jsonl
bun run vendor       # re-extract game formula core from the pinned tag
bun run typecheck && bun run typecheck:vendor && bun test
```

## Current intent (keep it simple)

The controller drives the HWGW engine (`shared/strategy/`) live, through
`game/lib/features/hacking.ts`. The other thirteen features are registered as
inert drivers gated on capabilities: they land one by one behind the seam
(`shared/world.ts`), each with a probe, a topic, a tab and a sim model, without
touching the loop.

Acquisition is never conditional. Every getter, dodge and probe runs in every
build and writes to `game/lib/state.ts`; `--perf` removes the telemetry send
and nothing else. See `spec/telemetry.md`.
