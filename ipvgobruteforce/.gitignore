# Committed: the C++ and TypeScript sources that regenerate everything below,
# plus the docs. The packed result the game runs is installed out of this
# directory to game/lib/generated/go-playbook.phase.js and is committed there.

# CMake output. `build-arena/` is the configured preset directory; the root
# .gitignore's `build/` does not match it.
/build/
/build-arena/

# The seeded-phase search output (28 GB) and the six certified v9.5 training
# exports it produces. Regenerated by ipvgo_seeded_batch / ipvgo_playbook_manifest
# / ipvgo_seeded_pack and arena/export-certified-v9.ts, per TRAINING_DATA.md, and
# moved between machines with go-ai/remote/worker.sh — never through git. Tests
# that read fixtures from here skip when it is absent.
/data/

*.snapshot
*.progress.tsv
/search.checkpoint
