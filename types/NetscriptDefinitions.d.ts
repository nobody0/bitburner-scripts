// Bootstrap placeholder. Run `bun run types` with Bitburner connected to replace
// this file with the exact definitions returned by the installed game.
export interface NS {
  tprint(...args: unknown[]): void;
}

